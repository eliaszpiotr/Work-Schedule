# Everything in requirements.txt, plus what the tests and the linter need.
-r requirements.txt

iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
ruff==0.16.2

# Packaging desktop builds; the release workflow installs this too.
pyinstaller==6.22.2
